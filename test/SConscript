Import('env')

env = env.Clone()

env.Append(LIBS=['gtest', 'pthread'])
bin = env.Program(target='test',
                  source=env.Glob('*.cpp'))

env.Command(target='run-test',
            source=bin[0],
            action=bin[0].abspath)

# vim:ft=python
